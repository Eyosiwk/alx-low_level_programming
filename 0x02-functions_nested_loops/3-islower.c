#include "main.h"

/**
 * _islower - a function that checks for lowercase character

 */
int _islower(int 1)
{
        if (1 >= 'a' && 1 <= 'z')
                return (1);
        else
                return (0);
}
